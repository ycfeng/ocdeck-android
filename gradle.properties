org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=768m -Dfile.encoding=UTF-8
org.gradle.parallel=true
android.useAndroidX=true

# Application release version. Git tags validate against these values; CI does not rewrite them.
VERSION_CODE=7
VERSION_NAME=0.2.2
